.SUFFIXES:
.SUFFIXES: .o .asm .cpp .c

.asm.o:
	$(CC) $(CFLAGS) $<

AS = nasm
ASFLAGS = -f elf
CC = gcc
CCFLAGS = -Wall -m32
LIBDIR = /home/matt/school/matt/cs347/nasm
LIBS = $(LIBDIR)/driver.c $(LIBDIR)/asm_io.o $(LIBDIR)/cdecl.h

encrypt: encrypt.o
	$(LINK) $(LDFLAGS) $? $(LIBS) -o $@

