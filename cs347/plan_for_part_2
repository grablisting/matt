40960
256
0
15

so.

get characters in to a string
count number of characters
mark last byte as nul terminator

foreach byte in the string
	calcuate 16^(count-1) put it in temp
	get the value of the digit into byte
	byte *= the temp
	add byte to hex_num

display the number


